//compress a string

function compress(message) {
    let compressedString = "";
    let messageArray = message.split("");

    for(let i =o; i < messageArray.length; i++) {
        let counter 
    }
}

console.log('hi');